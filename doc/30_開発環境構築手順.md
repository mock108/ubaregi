# ウバレジ 開発環境構築手順

| 項目 | 内容 |
| --- | --- |
| 文書番号 / 版 | 30 / 0.1 |
| 基準日 | 2026-09-07 |
| 開発者 | Mockup |
| 状態 | 段階3実装済み。debug APK / instrumented test APKをビルド済み。端末起動・接続テストは未実施 |
| 関連文書 | [基本設計書](10_基本設計書.md) / [詳細設計書](20_詳細設計書.md) / [公開・運用手順](40_公開・運用手順.md) |

## 1. 開発環境

Windows上のAndroid Studioで開発し、Android 17の実機とエミュレーターで確認する。開発者登録やGoogle Playへの接続は個人用アプリの開発条件にしない。

| 項目 | 採用値 |
| --- | --- |
| Android Studio | Quail 3 / 2026.1.3 Patch 1 |
| Android Gradle Plugin（AGP） | 9.3.2 |
| Gradle | 9.5.0。リポジトリに同梱するGradle Wrapperから実行 |
| Gradle実行用JDK | 17。Java / Kotlinの出力先も17に統一 |
| Android SDK | Android 17 / API 37 |
| SDK Build Tools | AGP既定の36.0.0。通常はbuildToolsVersionを明示しない |
| 言語・画面 | Kotlin、Jetpack Compose、Material 3 |
| DB | Room 2.8.4、KSP 2.3.11を固定。DBとコード生成を段階2で実装 |
| ビルド記述 | Kotlin DSL、バージョン管理はlibs.versions.tomlに集約 |

最新の開発ツールを追い続けず、上記の安定版の組合せを開始点とする。AGP 9.3の公式互換表はAPI 37・Gradle 9.5.0・JDK 17を示している。今回、AGP 9.3.2・Gradle 9.5.0・JDK 17で本アプリのビルドを確認した。[AGP公式情報](https://developer.android.com/build/releases/agp-9-3-0-release-notes) / [Android Studio公式情報](https://developer.android.com/studio/releases/past-releases/as-quail-3-release-notes)

AGP 9の組込みKotlinを使い、`org.jetbrains.kotlin.android`を重ねて適用しない。Compose Compilerプラグインと、採用する場合のSerializationプラグインは、解決されたKotlinのバージョンに揃える。RoomにはKAPTではなくKSPを使う。[組込みKotlin](https://developer.android.com/build/migrate-to-built-in-kotlin) / [Compose Compiler](https://developer.android.com/jetpack/androidx/releases/compose-kotlin) / [Room](https://developer.android.com/jetpack/androidx/releases/room)

Compose BOM、Lifecycle、KSP等の依存バージョンは、アプリプロジェクト作成時に上記環境でビルドできる安定版を実装担当が選定し、`gradle/libs.versions.toml`へ固定する。今回の固定値はCompose BOM 2026.08.00、Activity Compose 1.13.0、Lifecycle ViewModel Compose 2.10.0、Room 2.8.4、KSP 2.3.11、Compose Compiler Plugin 2.3.21、JUnit 4.13.2である。`+`や動的バージョンは使わない。

## 2. 環境導入時の手順

1. Android Studioを導入し、GitHubリポジトリの作業コピーを開く。
2. SDK ManagerでAndroid SDK Platform 37、Platform Tools、Build Tools 36.0.0、Android Emulatorを導入する。
3. Gradle実行用にJDK 17を指定する。Android Studio自身の実行用Javaとは区別する。
4. Device ManagerでAPI 37の安定版イメージを使う端末を作成する。課金を使わない個人用の確認ではPlay Store付きイメージは必須にしない。
5. Android 17の実機で開発者向けオプションとUSBデバッグを有効にし、本人の開発PCだけを許可する。

SDKの所在は各PCの`local.properties`に記録し、Gitへ登録しない。Android StudioやJDKが既にある場合は版と設定を確認して利用する。SDK・ライブラリの取得は開発作業の通信であり、インストール後のアプリのオフライン要件とは別である。

## 3. アプリ作成時の確定設定

単一の`app`モジュールと標準の`debug` / `release`を作成済みである。次の値を維持する。

| 設定 | 値・実装先 |
| --- | --- |
| namespace / 公開版applicationId | `io.github.mock108.ubaregi`、`app/build.gradle.kts` |
| debugのapplicationIdSuffix | `.debug`。個人用IDは`io.github.mock108.ubaregi.debug` |
| 表示名 | releaseは「ウバレジ」、debugは「ウバレジ（個人用）」 |
| versionName / versionCode | `0.1.0` / `1`。以後、配布更新ごとにcodeを増やす |
| minSdk / targetSdk / compileSdk | すべて37。maxSdkVersionは指定しない |
| 開発者名 | `Mockup`。アプリ情報の固定表示に使用 |
| 言語 / 金額 / 日時表示 | 日本語 / JPY整数 / Asia/Tokyo。保存時刻はUTC |
| 個人用の課金 | `BILLING_ENABLED = false`。購入ボタン・確認ボタン・課金SDK・購入用DBを設けない |
| 画面 | 4画面。Material 3標準部品、青系の単色アクセント、端末設定に従うライト / ダーク表示 |
| 保存 | アプリ内部のRoom DB。詳細設計書の形式を使用 |
| 外部保存 | OSの文書作成UI。広範なストレージ権限を要求しない |
| 個人用の権限 | インターネット、位置情報、連絡先、カメラ等を要求しない。問い合わせは外部ブラウザーに渡す |
| バックアップ | `allowBackup=false`に加え、クラウドバックアップ・端末間転送から内部データを除外するルールを設定 |

個人用の署名にはAndroid Studioが自動作成するデバッグ鍵を使う。今は公開用の鍵を生成しない。通常更新でデータを保持するため、個人用を使っている間は同じアプリIDとデバッグ鍵を維持する。[Androidの署名方式](https://developer.android.com/studio/publish/app-signing)

公開用の追加設定は[release.properties.example](../release.properties.example)に項目だけ用意する。現時点ではファイルを読むビルド処理はなく、個人用の実装・起動にもこの設定ファイルを要求しない。

## 4. 実装の順序

1. 4画面の遷移とアプリ情報、ローカルの説明表示を作る。
2. Roomと現金計算を実装し、開始・受渡し記録・補充・取出し・締めを通す。
3. 履歴の編集・取消・全クリア、再起動時の保持、二重保存防止を実装する。
4. 詳細設計書の独自CSV / JSON出力を実装する。
5. 実機で入力のしやすさ、文字拡大、機内モード、バックアップ除外を確認する。

ログイン、独自サーバー、広告、解析、DIフレームワーク、複数モジュール、外部会計APIは追加しない。画面はCompose、状態はViewModel、業務データはRoomという構成に留める。今は購入処理の空実装やSDKを先行追加する必要もない。

DBの版は1から開始する。RoomのスキーマJSONはGitに登録し、以後の更新では移行処理を作る。移行失敗時にDBを自動削除する設定は使わない。実際の金額や購入情報をログに出力しない。

## 5. プロジェクト作成後の確認

次のコマンドはアプリ本体とGradle Wrapperの作成後に、リポジトリ直下で実行する。現在の文書だけの状態では実行できない。

```powershell
.\gradlew.bat testDebugUnitTest lintDebug assembleDebug
.\gradlew.bat connectedDebugAndroidTest
```

後者はエミュレーターまたはUSB接続した実機を必要とする。テスト対象は[詳細設計書の第9節](20_詳細設計書.md#9-実装時の受入確認)とし、計算・保存・重複防止に意味のあるテストを用意する。初期インストールはAndroid StudioのRunから行い、以後の個人用APKは`app/build/outputs/apk/debug/`で確認する。

### 段階1の実施結果（2026-09-07）

- JDK 17、Gradle Wrapper 9.5.0、AGP 9.3.2、Android API 37 / Build Tools 36.0.0で実行した。
- `testDebugUnitTest`、`lintDebug`、`assembleDebug`は成功した。
- 生成物は`app/build/outputs/apk/debug/app-debug.apk`。debugのアプリIDは`io.github.mock108.ubaregi.debug`、versionNameは`0.1.0`、versionCodeは`1`。
- `adb devices`は接続端末なしだったため、実機・エミュレーターの起動と4画面の目視確認は未実施である。
- Android Studio本体はこの開発環境に導入されていない。Android Command-line Toolsでビルドを確認したため、本人がAndroid Studioで画面確認する場合は設計表の採用版を導入する。

### 段階2の実施結果（2026-09-07）

- Room 2.8.4、KSP 2.3.11、DBスキーマ版1を追加し、`app/schemas/`へスキーマJSONを生成した。
- `assembleDebug` と `assembleDebugAndroidTest` は成功した。debug APKは`app/build/outputs/apk/debug/app-debug.apk`。
- 計算テスト5件は、生成済みクラスをJUnitで直接実行し成功した。RoomインメモリDBによるT-35テストも追加し、instrumented test APKへコンパイル済みである。
- `connectedDebugAndroidTest`は「No connected devices」で実行できなかったため、T-35の実DB実行と再起動後保持は未確認として残す。Gradleのunit testタスクはこの環境のKotlinテスト出力探索制限で結果を取得できなかった。

### 段階3の実施結果（2026-09-07）

- レジ履歴、受渡し履歴、補充・取出し履歴の表示・編集・取消、全履歴クリアを実装した。取消済み明細は元金額を残し、集計から除外する。
- 締め済みレジの新規明細は保存層と画面の両方で拒否し、締め後の既存履歴編集は親レジの集計だけを更新する。後続レジの初期釣銭は自動変更しない。
- 編集・取消は親レジrevisionと明細revisionを照合し、古い画面からの更新を競合として拒否する。全クリアは全体版番号を照合し、下書き入力・保存ID・選択中レジを破棄してホームへ戻す。
- 計算テスト5件を生成済みクラスでJUnit直接実行し成功した。`assembleDebug` と `assembleDebugAndroidTest` は成功した。追加した段階3のRoomテストはinstrumented test APKへコンパイル済みである。
- `connectedDebugAndroidTest`は接続端末がないため未実施であり、段階3の実機画面確認、再起動後保持、Room実行、T-35の実DB実行は未確認として残す。

必要な依存の取得後、アプリを機内モードで確認する。開発用の依存解決と、利用者の端末でのアプリ通信を取り違えない。インストール失敗時に、データを失うアンインストールを最初の対処にしない。

## 6. リポジトリで管理するもの

ソース、設計書、Gradle Wrapper、固定した依存バージョン、Roomスキーマ、架空データのテストを管理する。`.editorconfig`と`.gitattributes`で文字コード・改行を揃える。

SDKパス、IDE設定、ビルド成果物、署名鍵、公開設定の実体、実際の配達記録は`.gitignore`の対象にする。手元の出力はリポジトリ外、または無視対象の`private/`・`exports/`に保管する。公開前に差分を確認し、秘密を含むファイルを強制追加しない。

この文書の環境値は固定済みである。段階3の実装結果を記録したが、実機試験・個人運用前の受入確認は未実施である。実機またはエミュレーターで開始から締め、履歴編集・取消・全クリア、再起動後の保持、文字の読みやすさ、機内モード時の画面確認を終えてから、[公開・運用手順](40_公開・運用手順.md)の個人運用開始条件へ進む。
